# Netlify Redirects file
# 1. Proxy all /api/* requests to the live backend server
/api/*  https://emad-6414.ai.studio/api/:splat  200

# 2. SPA fallback for frontend routes (e.g. /about, /portfolio, /admin)
/*  /index.html  200

